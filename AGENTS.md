# FrameworkX Agent 规则

FrameworkX 是跨产品公共 SDK，不是业务应用。

## 架构规则

- 架构优先，功能其次；新增能力前先定义所有权、端口、适配器和生命周期。
- 公共包不得依赖 ViewX 或其他宿主的页面、路由、Bloc、数据库表和业务模型。
- 核心层不得依赖 HTTP 框架、数据库驱动或平台实现；具体技术放入独立适配器。
- 宿主通过构造参数、接口或事件接入，不允许 SDK 反向访问宿主全局对象。
- 跨端字符串协议必须在 `contracts/` 中拥有明确契约，避免客户端和服务端各自猜测。
- 每个发布包保持小而稳定的公开 API，破坏性变更必须升级主版本。

## 迁移规则

- 迁移前确认原代码的权威仓库和许可证。
- 先迁移抽象与测试，再迁移默认适配器。
- 不携带宿主密钥、环境配置、生成缓存和业务迁移数据。
- 迁移完成前，原业务仓库继续作为运行来源；不得制造两个同时可修改的权威副本。

