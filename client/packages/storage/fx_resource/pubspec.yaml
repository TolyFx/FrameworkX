name: fx_resource
description: 面向 Flutter 应用的本地资源引用、选择与路径迁移基础包。
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.1
  flutter: '>=3.27.0'

dependencies:
  file_picker: ^8.0.0
  flutter:
    sdk: flutter
  fx_exception: ^0.0.1+4
  path_provider: ^2.1.4

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
