name: fx_user_device
description: FrameworkX 用户登录审计设备信息适配器。
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^10.1.2
  package_info_plus: ^8.3.1
  shared_preferences: ^2.5.3
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
