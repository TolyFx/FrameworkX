# fx_storage_oss — S3 兼容/阿里云 OSS 存储后端(实现 StorageBackend,暴露 client 供 STS/HeadObject)。
[package]
name = "fx_storage_oss"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
fx_storage_core = { path = "../../fx_storage_core" }
aws-config = "1"
aws-sdk-s3 = "1"
aws-credential-types = "1"
aws-types = "1"

[lints]
workspace = true
